import { dgidb as SOURCE_DEFN } from "../sources";
export function upload({ conn, url }: {
    conn: any;
    url?: string;
}): Promise<void>;
export declare const dependencies: string[];
export { SOURCE_DEFN };
