import { cgi as SOURCE_DEFN } from "../sources";
export function uploadFile({ conn, filename, errorLogPrefix }: {
    conn: any;
    filename: any;
    errorLogPrefix: any;
}): Promise<void>;
export { SOURCE_DEFN };
